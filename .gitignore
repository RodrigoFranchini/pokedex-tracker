# Temporary working notes, not documentation. Each is a checklist for one task
# and must be deleted once that task is done -- see the banner at the top of the
# file. Nothing tracked should ever link to one.
/vercel.md
